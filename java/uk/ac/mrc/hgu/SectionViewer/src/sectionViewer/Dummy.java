package sectionViewer;
import sectionViewer.*;

/**
 *   Empty class that exists so that the full path to the SectionViewer help system
 *   can be determined by reflection.
 *   It is assumed that the help files are in the same directory as 
 *   the instance of 'Dummy'.
 *   This is necessary when the application is run from a jar file
 *   (as is the case when it is started via Java Web Start).
 */
public class Dummy {
}
